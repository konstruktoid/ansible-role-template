on: [push, pull_request]
name: Ansible Lint

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3.5.2
      - name: Lint Ansible Playbook
        uses: ansible/ansible-lint-action@9b022b66407018ebb6d44c904f4a386766d34368 # main
